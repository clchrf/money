# Supabase —— 從 Dashboard → Project Settings → API 取得
#
# 這兩個值可以安全地放進前端：anon key 是設計成公開的，
# 真正的保護來自資料庫的 Row Level Security（見 supabase/schema.sql）。
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# ⚠️ 絕對不要在這個檔案、或任何 VITE_ 開頭的變數放：
#      service_role key / Email API key / 任何 secret
#
#    所有 VITE_ 變數都會被打包進前端 bundle，等同公開。
#    Server-side secrets 只能放在 Supabase Edge Function Secrets，
#    或 Hosting 平台的（非 VITE_ 前綴）環境變數。
